<Project>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
  </PropertyGroup>

        <Target Name="CBA">
        <Message Importance="high" Text="##teamcity[buildNumber 'ha']" />
    </Target>
</Project>
